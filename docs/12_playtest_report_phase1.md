# 阶段 1 垂直切片 — 试玩报告

## 基本信息

- **版本**：阶段 1.1（切片打磨与结构固化）
- **测试日期**：2026-05-29
- **测试方式**：Godot 4.6 编辑器内 F5 运行
- **测试范围**：一分钟垂直切片完整流程

## 阶段 1.1 修改清单

| 文件 | 改动 | 说明 |
|------|------|------|
| `scripts/calibration_screen.gd:32` | `btn` → `_btn` | 修复 `UNUSED_PARAMETER` 编译警告 |
| `scripts/station_scene.gd` | 7 处文本重写 | 统一称呼 + 打磨初始文本 + 打磨调查文本 |
| `scenes/station_scene.tscn` | 3 处文本重写 | 按钮名 + ChoiceD + 初始文本默认值同步 |

### 改动详解

**① 修复编译警告**
- `calibration_screen.gd:32` — 未使用参数 `btn` 加下划线前缀 `_btn`

**② 统一称呼**
- 交互按钮：`公告牌` → `电子公告屏`
- 叙事文本：`公告牌` → `公告屏`
- 系统反馈文本：`公告牌显示` → `电子公告屏显示`

**③ 打磨初始文本**
```
旧：你站在一座空车站里。公告牌亮着，广播没有声音。时钟显示 23:47。
新：你站在一座空车站里。公告屏仍在刷新。广播灯亮着，没有声音。时钟停在 23:47。
```

**④ 打磨调查文本**
```
旧：公告牌显示：末班车即将进站。下一行短暂闪了一下：{keyword}已载入。
新：电子公告屏显示：末班车即将进站。第二行刷新了一次：{keyword}已载入。
```

## 试玩流程记录

### 校准界面

| 步骤 | 操作 | 结果 |
|------|------|------|
| 1 | 启动项目，检查 Output | 无 `UNUSED_PARAMETER` 警告，无红色 error |
| 2 | 点击选项 A / B / C / D | 按钮禁用，显示"校准完成。正在生成场景……" |
| 3 | 等待 1.5 秒 | 自动切换到车站场景 |

### 车站场景

| 步骤 | 操作 | 结果 |
|------|------|------|
| 4 | 观察初始文本 | `你站在一座空车站里。公告屏仍在刷新。广播灯亮着，没有声音。时钟停在 23:47。` |
| 5 | 点击"电子公告屏"按钮 | 调查文本：`电子公告屏显示：末班车即将进站。第二行刷新了一次：{hint}。`，主线选项出现 |
| 6 | 点击主线选项 | 按钮禁用，反馈文本正常，广播灯变色 |
| 7 | 再次点击"电子公告屏" | `电子公告屏显示：23:47 末班车 目的地：校准中 上一行为：{choice}` |

### 四轮交叉测试

| 校准选 → 主线选 | 公告屏初次调查文本 | 广播灯色 | 公告屏残影 |
|----------------|-------------------|---------|-----------|
| A(灯) → A(询问) | 灯光参数已载入 | 橙色 | 上一行为：询问广播 |
| B(门) → B(查看) | 出口状态已载入 | 蓝色 | 上一行为：查看出口 |
| C(广播) → C(等待) | 广播记录已载入 | 绿色 | 上一行为：继续等待 |
| D(车票) → D(沉默) | 日期偏差已载入 | 琥珀 | 上一行为：保持沉默 |

## 安全检查

| 检查项 | 结果 |
|--------|------|
| 主线按钮禁用后不可重复点击 | ✅ |
| 电子公告屏点击不修改状态值 | ✅ |
| 所有节点路径正确，无 `Invalid get index` | ✅ |
| 场景切换正常，GameState 值跨场景保留 | ✅ |
| 无编译警告（`UNUSED_PARAMETER` 已修复） | ✅ |
| 无红色 error 输出 | ✅ |

## 结论

- **功能**：✅ 全部通过
- **文本**：✅ 称呼统一（公告牌→公告屏/电子公告屏），初始文本和调查文本已打磨为更冷、更准、更系统化的风格
- **编译警告**：✅ `UNUSED_PARAMETER` 已修复
- **性能**：✅ 反应快速，无卡顿
- **安全**：✅ 无状态刷值漏洞，无红色报错
- **状态**：**可进入下一阶段**
